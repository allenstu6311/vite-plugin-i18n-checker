---
alwaysApply: true
---
description: "測試規範：定義測試案例的命名、結構、風格與組織原則"
globs:
  - "test/**/*.test.ts"
---

# 🧪 測試規範

本規範定義此專案中所有測試案例應遵循的標準，確保測試案例的一致性與可維護性。

---

# 📁 檔案命名規範

## 檔案命名格式
- 使用 **kebab-case** 命名測試檔案
- 檔案名稱需清楚描述測試範圍
- 統一使用 `.test.ts` 作為副檔名

### 檔案命名範例
```
basic.test.ts           # 基本功能測試
nested.test.ts          # 巢狀結構測試
complex.test.ts         # 複雜情況測試
edge-cases.test.ts      # 邊境情況測試
array.test.ts           # 陣列相關測試
spread.test.ts          # Spread 操作測試
variables.test.ts       # 變數相關測試
```

---

# 🏗️ 測試檔案結構

## 標準測試檔案模板

```typescript
import { /* 導入需要測試的模組 */ } from '@/module/path';
import { describe, expect, it, beforeEach } from 'vitest';

/**
 * [測試套件名稱]
 * [簡要說明此測試檔案的目的與涵蓋範圍]
 */
describe('[測試套件名稱]', () => {
    // 可選：測試前置設定
    beforeEach(() => {
        // 初始化設定
    });

    it('[測試案例描述]', () => {
        // 測試實作
    });
});
```

---

# 📝 測試描述規範

## describe 命名規範
- 使用 **繁體中文** 描述測試套件
- 格式：`[功能名稱] 測試`
- 範例：`diff 基本比對測試`、`extract 基本功能測試`

## it 命名規範
- 使用 **繁體中文** 描述單個測試案例
- 清楚描述測試的預期行為或情境
- 使用動詞開頭，描述「應該」、「能夠」、「檢測」等行為
- 邊境測試以「邊境：」為前綴

### 命名範例
```typescript
it('完全相同的物件應該返回空結果', () => { ... });
it('檢測缺少的鍵值', () => { ... });
it('邊境：空物件與空陣列比對', () => { ... });
it('三層巢狀物件比對', () => { ... });
```

---

# 🎯 測試分類規範

## 基本測試分類
根據測試內容的複雜度與特性，將測試檔案分類如下：

### 1. basic.test.ts
- **用途**：測試核心功能的基本操作
- **內容**：涵蓋最常見的使用情境、基本輸入輸出
- **範例**：基本物件比對、基本類型解析

### 2. nested.test.ts
- **用途**：測試巢狀結構的處理
- **內容**：深層巢狀物件、混合巢狀結構
- **範例**：三層巢狀物件、陣列中巢狀物件

### 3. complex.test.ts
- **用途**：測試複雜場景與組合情況
- **內容**：多種情況同時發生、複雜邏輯流程
- **範例**：多種異常類型組合、大型結構處理

### 4. edge-cases.test.ts
- **用途**：測試邊境情況與極端輸入
- **內容**：空值處理、特殊字符、極端深度/寬度、特殊型別
- **範例**：null/undefined 處理、空字串鍵值、極深巢狀結構

### 5. 特定功能測試
- **用途**：針對特定功能或特性的專門測試
- **命名**：以功能名稱命名（如 `array.test.ts`、`spread.test.ts`）
- **內容**：聚焦於該功能的各種情況

---

# ✍️ 測試程式碼風格

## 變數命名
- 使用 **camelCase** 命名變數
- 變數名稱需清楚表達其用途
- 測試資料使用有意義的名稱，避免 `a`、`b`、`test` 等過於簡單的名稱

### 良好範例
```typescript
const source = { name: 'John', age: 25 };
const target = { name: 'John', age: 25, email: 'john@example.com' };
const abnormalKeys = { missingField: AbnormalType.MISS_KEY };
```

### 避免範例
```typescript
const a = { x: 1 };
const b = { x: 1, y: 2 };
```

## 測試註解
- 檔案開頭使用 **JSDoc 註解** 說明測試目的
- 複雜測試邏輯需加上行內註解
- 邊境測試需說明測試的特殊性

### JSDoc 範例
```typescript
/**
 * diff 基本比對測試
 * 測試 diff 函數的基本功能，包括相同物件、缺少鍵值、額外鍵值等基本情況
 */
```

### 邊境測試註解範例
```typescript
it('邊境：空物件與空陣列比對', () => {
    // 測試空物件與空陣列的類型差異處理
    ...
});
```

---

# 🧩 測試內容規範

## 必須涵蓋的測試類型

### 1. 基本功能測試
- 正常流程的預期行為
- 最常見的使用情境
- 基本輸入輸出的正確性

### 2. 異常情況測試
- 缺少必要資料的處理
- 額外資料的處理
- 結構不匹配的處理

### 3. 邊境情況測試
- 空值處理（null、undefined、空物件、空陣列）
- 特殊字符鍵值
- 極端深度或寬度的資料結構
- 特殊型別（函數、日期、正則表達式）

### 4. 巢狀結構測試
- 多層巢狀物件的處理
- 陣列中巢狀物件的處理
- 混合巢狀結構（物件與陣列混合）

---

# 🔧 測試前置設定

## beforeEach 使用規範
- 當測試需要共用初始化邏輯時使用 `beforeEach`
- 設定應放在測試套件的最上方
- 初始化邏輯需清楚說明其目的

### 範例
```typescript
describe('processAbnormalKeys 異常分類測試', () => {
    let abnormalManager: AbnormalState;

    beforeEach(() => {
        abnormalManager = createAbormalManager();
    });

    it('缺少鍵值應該歸類到 missingKey', () => {
        // 使用 abnormalManager 進行測試
    });
});
```

---

# ✅ Assertion 規範

## 使用 Vitest 的 expect
- 優先使用語意明確的 matcher
- 使用 `toEqual` 進行深度比較（物件、陣列）
- 使用 `toBe` 進行淺層比較（基本型別、引用）
- 使用 `toHaveLength` 檢查陣列長度

### 範例
```typescript
expect(result).toEqual({ a: 'hello' });           // 物件深度比較
expect(array).toHaveLength(3);                    // 陣列長度檢查
expect(value).toBe(null);                          // 基本型別比較
expect(missingKey).toHaveLength(1);               // 陣列長度檢查
expect(missingKey[0]).toEqual({                    // 物件深度比較
    filePaths: 'test.ts',
    key: 'missingField'
});
```

---

# 📊 測試資料規範

## 測試資料設計原則
- 測試資料應具代表性，涵蓋真實使用場景
- 使用清楚命名，避免魔術數字或字串
- 複雜測試資料可使用輔助函數建立

### 良好範例
```typescript
const createDeepObject = (depth: number): any => {
    if (depth <= 0) return 'leaf';
    return {
        level: depth,
        children: createDeepObject(depth - 1)
    };
};

const source = createDeepObject(10);
```

---

# 🚫 禁止事項

## 避免的做法
1. ❌ 使用過於簡單的變數名稱（如 `a`、`b`、`test`）
2. ❌ 測試描述使用英文混合中文（應統一使用繁體中文）
3. ❌ 缺少檔案開頭的 JSDoc 註解
4. ❌ 測試邏輯過於複雜，應拆分為多個測試案例
5. ❌ 在測試中使用真實的外部資源（檔案、網路請求）
6. ❌ 測試之間有依賴關係（每個測試應獨立）

---

# 📋 檢查清單

在撰寫測試前，請確認：

- [ ] 測試檔案名稱使用 kebab-case 且以 `.test.ts` 結尾
- [ ] 檔案開頭有 JSDoc 註解說明測試目的
- [ ] `describe` 和 `it` 的描述使用繁體中文
- [ ] 測試案例涵蓋基本功能、異常情況、邊境情況
- [ ] 變數命名清楚表達用途，使用 camelCase
- [ ] 使用適當的 assertion matcher（toEqual、toBe、toHaveLength）
- [ ] 測試案例之間相互獨立，無依賴關係
- [ ] 複雜測試邏輯有適當註解說明

---

# 🤖 AI 行為規範

當 AI 協助撰寫測試時，應遵循：

1. **測試檔案分類**：根據測試內容選擇適當的分類（basic、nested、complex、edge-cases）
2. **完整覆蓋**：確保涵蓋基本功能、異常情況、邊境情況
3. **風格一致**：遵循既有的命名與結構風格
4. **清楚描述**：測試描述清楚表達測試目的與預期行為
5. **避免冗長**：測試邏輯保持簡潔，複雜邏輯拆分成多個測試案例